---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  namespace: argocd
  name: id
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    name: in-cluster
    namespace: id
  syncPolicy:
  #  automated:
  #    prune: true
  #    selfHeal: true
    syncOptions:
      - CreateNamespace=true
  sources:
    - repoURL: https://github.com/bacchus-snu/cd-manifests.git
      targetRevision: main
      path: argocd/atrium/id
      kustomize: {}
    - repoURL: registry-1.docker.io/bitnamicharts
      targetRevision: 28.2.1
      chart: redis
      helm:
        valueFiles:
          - $values/argocd/atrium/id/redis-values.yaml
    - repoURL: registry-1.docker.io/bitnamicharts
      targetRevision: 18.11.3
      chart: postgresql
      helm:
        valueFiles:
          - $values/argocd/atrium/id/postgresql-values.yaml
    - repoURL: https://github.com/bacchus-snu/cd-manifests.git
      targetRevision: main
      ref: values
