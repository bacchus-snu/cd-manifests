---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  namespace: argocd
  name: zest-atrium-bootstrap
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: zest
  destination:
    name: zest
    namespace: default
  syncPolicy:
    #automated:
    #  prune: true
    #  selfHeal: true
  sources:
    - repoURL: https://github.com/bacchus-snu/cd-manifests.git
      targetRevision: main
      path: argocd/zest/atrium-bootstrap
      directory:
        include: 'resources.yaml'
